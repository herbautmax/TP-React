'use strict';

module.exports = {
    DEFAULT_ARTICLES: [
        {id: 0, name: 'product1', unitPrice: (Math.random() * 100).toFixed(2), quantity: 1},
        {id: 1, name: 'product2', unitPrice: (Math.random() * 100).toFixed(2), quantity: 1}
    ]
};